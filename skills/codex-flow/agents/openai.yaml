interface:
  display_name: "Codex Flow"
  short_description: "Plan with Sol, execute with the right model"
  default_prompt: "Use $codex-flow handoff to prepare and confirm an execution handoff."
policy:
  allow_implicit_invocation: true
